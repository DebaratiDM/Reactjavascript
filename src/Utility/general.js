export function areaOfReactangle(length, width) {
  const area = length * width;
  return area;
}
export function areaOfCircle(Pie, radius) {
  const areaOfCircle = Pie * radius * radius;
  return areaOfCircle.toFixed(2);
}
